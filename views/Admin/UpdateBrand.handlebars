{{#section 'js'}}
<script>
    $('#submit').click(function (event) {
        var txtID = $('#ID').val();
        var txtName = $('#Name').val();
        if (txtID === '' || txtName === '') {
            event.preventDefault();
        }
    })
</script> {{/section}}
<div style="text-align:center; font-size:40px; font-weight:bold; font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif">&nbspEditing Brand</div>
<br>
<br> {{#with Brand}}
<div class="row">
    <div class="col-md-4 col-md-offset-4">
        <form method="POST">
            <div class="form-group">
                <label for="ID">
                    <strong>Brand ID</strong>
                </label>
                <input type="text" class="form-control" id="ID" name="ID" value="{{brand_id}}" readonly>
            </div>
            <div class="form-group">
                <label for="Name">
                    <strong>Brand name</strong>
                </label>
                <input type="text" class="form-control" id="Name" name="Name" value="{{brand_name}}" maxlength="30">
            </div>
            <div class="text-center">
                <button class="btn btn-success" id="submit" type="submit">Save</button>
            </div>
        </form>
    </div>
</div>
{{/with}}